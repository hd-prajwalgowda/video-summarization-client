import React from 'react';

const Navbar = ({ title, onClick, icon }) => {
  return (
    <button  className="py-1 px-3 border-2 border-blue-500 rounded-sm text-blue-500">
      Click
    </button>
  );
};

export default Navbar;
